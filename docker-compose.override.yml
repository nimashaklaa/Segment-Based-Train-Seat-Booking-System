services:
  frontend:
    build:
      context: ./frontend
      target: builder
    command: npm run dev -- --host --port 80
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      VITE_API_URL: http://localhost:3000
